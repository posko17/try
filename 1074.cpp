#include <iostream>
using namespace std;
int check_quadrant(int r, int c, int size,int x,int y,int count);
int main()
{
	int n,r,c,count=0;
	long long int size=1;
	//cin.tie(NULL);
	//ios_base::sync_with_stdio(false);
	cin>>n>>r>>c;
	for(int i=0;i<n;i++)
		size=size*2;
	//check the quadrant
	check_quadrant(r,c,size/2);
}
int check_quadrant(int r, int c,int size,int x, int y,int count)
{
	int quadrant
	if(size==2)
	{

	}
	else
	{

	}
}